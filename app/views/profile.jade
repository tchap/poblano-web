extend layout
block menu
  ul.nav
    li
      a(href='#{prefix}/project/initialise') Initialise a Project
  div.pull-right
    p.btn.btn-link
      a(href='#{prefix}/user/#{user.id}')= user.displayName
block content
  h1= user.displayName
  p Under construction...
